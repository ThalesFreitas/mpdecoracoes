{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar slug: </h3>

<div class="card">
    <div class="card-body">
<form action="/admin/slugtemas/edit" method="POST">
<input type="hidden" name="id" value="{{slugtema._id}}">
<label for="nome">Nome: </label>
<input type="text" name="slugtema" id="slugtema" placeholder="Nome da categoria" class="form-control" value="{{slugtema.slugtema}}">

<br>
<button type="submit" class="btn btn-success">Editar slug</button>
</form>
    </div>
</div>
